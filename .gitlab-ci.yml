build:
  stage: build
  tags: 
    - dockerfasibio
  image: docker
  script:
    - dockerize/buildDocker.sh latest
  only:
    - master
publish:
  stage: publish
  tags: 
    - dockerfasibio
  image: docker
  script:
    - dockerize/publish.sh latest
  only:
    - master
stages: 
  - build
  - publish